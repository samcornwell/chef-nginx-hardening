server {
	<% @listen.each do |param| %>
	  listen <%= param %>;
	<% end %>
    root    <%= @docroot %>;

    location /application {
      if ($request_method !~ ^(GET|PUT|POST)$ ) {
      return 444;
      }

      random_index off;
    }
  }